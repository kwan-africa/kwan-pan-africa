{
  "version": 2,
  "buildCommand": "cd kwan-web-app && npm install && npm run build",
  "outputDirectory": "kwan-web-app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
